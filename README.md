# IceControl — Backend

Scaffold backend for IceControl (Node.js, Express, TypeScript, Prisma, PostgreSQL).

See `.env.example` for required env vars. Use `npm install` then `npm run dev` to start in development.
